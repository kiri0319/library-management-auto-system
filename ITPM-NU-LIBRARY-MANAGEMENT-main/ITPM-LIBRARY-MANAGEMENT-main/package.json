{
  "name": "online-library-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node ./node_modules/concurrently/dist/bin/concurrently.js \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "server": "npm run dev --workspace server",
    "client": "npm run dev --workspace client",
    "seed": "npm run seed --workspace server",
    "build": "npm run build --workspace client",
    "start": "node ./node_modules/concurrently/dist/bin/concurrently.js \"npm run start --workspace server\" \"npm run preview --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
